<section id="waitlist" class="mx-auto max-w-6xl px-4 py-10">
  <div class="rounded-2xl border p-6">
    <h2 class="text-xl font-semibold">Join the waitlist</h2>
    <p class="mt-2 text-sm text-neutral-700">Be first to access the individual leadership assessment preview.</p>
    <form name="waitlist" method="POST" data-netlify="true" netlify-honeypot="bot-field" action="/thanks" class="mt-4 grid gap-3 md:grid-cols-[1fr_auto]">
      <input type="hidden" name="form-name" value="waitlist" />
      <p class="hidden">
        <label>Don’t fill this out: <input name="bot-field" /></label>
      </p>
      <input required name="email" type="email" placeholder="you@example.com" class="w-full rounded-xl border px-3 py-2" />
      <button class="rounded-xl bg-black px-4 py-2 text-white">Join</button>
    </form>
  </div>
</section>
